# Codex action test bench

The implementation is canonical in [ha-mcp](https://github.com/homeassistant-ai/ha-mcp/tree/ci/generic-codex-action/.github/actions/codex-run).

Bench workflows check out an explicit full commit SHA instead of maintaining an
independent copy. See [the bench runbook](../../../fixtures/README.md).
