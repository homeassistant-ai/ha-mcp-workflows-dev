name: Codex example - hello world

on:
  workflow_dispatch:
    inputs:
      action-ref:
        description: Trusted full commit SHA of the canonical ha-mcp actions.
        required: true
        default: "d555ca0fc1a0fc2febf0648b9d92a194fe5b85d3"
        type: string
      model:
        description: Explicit Codex model to test.
        required: true
        default: gpt-6-astra
        type: string

permissions:
  contents: read

concurrency:
  group: codex-auth-${{ github.repository }}
  cancel-in-progress: false
  queue: max

jobs:
  hello-world:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        timeout-minutes: 2
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Prepare canonical actions
        timeout-minutes: 5
        uses: ./.github/actions/prepare-subject
        with:
          action-ref: ${{ inputs.action-ref }}

      - name: Run arbitrary instructions
        timeout-minutes: 18
        id: codex
        uses: ./.test-subject/.github/actions/codex-run
        with:
          codex-auth: ${{ secrets.CODEX_AUTH }}
          model: ${{ inputs.model }}
          reasoning-effort: low
          sandbox: read-only
          timeout-minutes: "5"
          instructions: |
            Run this shell command exactly once:

            printf 'Hello world'

            Then respond with exactly its stdout and nothing else.

      - name: Verify the caller's expectation
        timeout-minutes: 1
        env:
          OUTPUT_PATH: ${{ steps.codex.outputs.output-path }}
        run: |
          set -euo pipefail
          normalized="$(mktemp "$RUNNER_TEMP/codex-output-normalized.XXXXXX")"
          expected="$(mktemp "$RUNNER_TEMP/codex-output-expected.XXXXXX")"
          tr -d '\r' < "$OUTPUT_PATH" > "$normalized"
          printf 'Hello world' > "$expected"
          if ! cmp -s "$expected" "$normalized"; then
            echo "::error::Codex output did not exactly match 'Hello world'."
            exit 1
          fi

      - name: Validate secret-writer PAT and persist auth
        timeout-minutes: 3
        if: ${{ always() && steps.codex.outputs.auth-path != '' }}
        uses: ./.test-subject/.github/actions/codex-update-auth
        with:
          codex-auth-path: ${{ steps.codex.outputs.auth-path }}
          original-codex-auth: ${{ steps.codex.outputs.original-auth-path }}
          gh-token: ${{ secrets.CODEX_AUTH_PAT }}
          repository: ${{ github.repository }}
          force-update: "true"
